const Footer = () => {
    return (  
        <footer>All data comes from Steams API's, Web Scraping, and external API's. If those respected websites/apis are down data with not be shown.</footer>
    );
}
 
export default Footer;