const Footer = () => {
    return (
        <footer className="text-gray-alt-txt text-xs text-center my-12">
            Notice: All data comes from Steams API's, external API's, and scraped from websites. If any of those
            services are down, data with not be shown.
        </footer>
    );
};

export default Footer;
